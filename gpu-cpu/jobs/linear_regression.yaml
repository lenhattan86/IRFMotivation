apiVersion: v1
kind: Pod
metadata:
  name: irf-gpu  
spec:  
  restartPolicy: Never  
  volumes:
  - name: nvidia-driver-375-82
    hostPath:
      path: /usr/lib/nvidia-375
  - name: libcuda-so
    hostPath:
      path: /usr/lib/x86_64-linux-gnu/libcuda.so
  - name: libcuda-so-1
    hostPath:
      path: /usr/lib/x86_64-linux-gnu/libcuda.so.1
  - name: libcuda-so-375-82
    hostPath:dd
      path: /usr/lib/x86_64-linux-gnu/libcuda.so.375.82  
  containers:
  - name: tfgpu
    image: swiftdiaries/kubernetes-demo:1.1.3
    command:
    - "/bin/bash"
    - "-c"
    - "python linear_regresion.py"  
    resources:
      requests:
        alpha.kubernetes.io/nvidia-gpu: 1
        cpu: 1
        memory: "32Gi"
      limits:
        alpha.kubernetes.io/nvidia-gpu: 1
        cpu: 1
        memory: "32Gi"
    volumeMounts:
    - name: nvidia-driver-375-82
      mountPath: /usr/local/nvidia
      readOnly: true
    - name: libcuda-so
      mountPath: /usr/lib/x86_64-linux-gnu/libcuda.so
    - name: libcuda-so-1
      mountPath: /usr/lib/x86_64-linux-gnu/libcuda.so.1
    - name: libcuda-so-375-82
      mountPath: /usr/lib/x86_64-linux-gnu/libcuda.so.375.82     
  